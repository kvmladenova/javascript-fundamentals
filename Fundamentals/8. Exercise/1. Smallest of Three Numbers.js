function smallest (a, b, c){
    //  let arr = [a, b , c];
    // let minNum = arr[0];
    // for (let i = 0; i <arr.length; i ++){
    //     if (arr[i] < minNum){
    //         minNum = arr[i];
    //     }
    // }
    // console.log(minNum);
    let minNum = Math.min(a, b, c);
    console.log(minNum);
}
smallest (2, 5, 3);
smallest (600, 342, 123);