function gladiatorExpenses (a,b,c,d,e,f){
    let lostFightCount = Number (a); //загубени битки
    let helmetPtice = Number (b); //цена на каската
    let swordPrice = Number (c); //цена на меча
    let shieldPrice = Number (d); //цена на щита
    let armorPrice = Number (f); //цена на бронята

    let helmetCrushCount = 0
    for (let i = 0; i <= helmetCrushCount; i++){
        if (lostFightCount > 2){
        lostFightCount % 2 == 0
        helmetCrushCount++;
    }
    }
    console.log(helmetCrushCount);
}
gladiatorExpenses ('7','2','3','4','5');
gladiatorExpenses ('23','12.50','21.50','40','200');